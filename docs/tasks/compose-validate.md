# `task compose:validate`

## What it does

Runs [`test`](./compose-test.md).

## Why it exists

Naming consistency with every other component's `*:validate` task — the
`compose` boundary of `task validate`.

## How to use it

```bash
task compose:validate
```

## Related

* [Task Reference index](../tasks-reference.md)
* [`task compose:test`](./compose-test.md)
* [`task validate`](./validate.md)
