apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.service.name }}
  labels:
    {{- include "pulse.labels" . | nindent 4 }}
    {{- include "pulse.service.selectorLabels" . | nindent 4 }}
spec:
  selector:
    {{- include "pulse.service.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
